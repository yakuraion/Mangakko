query QueryMediaDetails($id: Int!) {
    Media(id: $id) {
        id
        title {
            romaji
        }
        type
        coverImage {
            extraLarge
            color
        }
        meanScore
        description(asHtml: true)
        rankings {
            allTime
            type
            rank
        }
    }
}
