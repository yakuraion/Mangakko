query QueryMediaDetails($id: Int!) {
    Media(id: $id) {
        id
        title {
            romaji
        }
        coverImage {
            extraLarge
            color
        }
        meanScore
        description
    }
}
